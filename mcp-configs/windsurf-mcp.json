{
  "mcpServers": {
    "plan-cascade": {
      "command": "python",
      "args": ["-m", "mcp_server.server"],
      "cwd": "{{PLAN_CASCADE_PATH}}",
      "env": {
        "PYTHONPATH": "{{PLAN_CASCADE_PATH}}"
      }
    }
  }
}
